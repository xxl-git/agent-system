{
  "name": "@agent-system/core",
  "version": "0.9.2",
  "description": "Agent core — main loop, adapter, orchestrator, context manager, intent parser",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rmdir /s /q dist 2>nul & exit /b 0"
  },
  "license": "MIT"
}
