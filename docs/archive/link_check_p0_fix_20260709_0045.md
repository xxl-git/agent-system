# 全链路运行评估 + P0 修复 — 2026-07-09 00:45

## 评估目标

用户要求对 agent-system 项目做全链路运行评估，验证从启动到聊天回复的完整调用链是否通畅。

## 评估方法

1. 启动 agent-server + LM Studio（qwen3.6-35b-a3b 模型）
2. 逐层验证各 API 端点（dashboard/config/models/sessions/context/chat/stream）
3. 实际发送 chat 请求测试端到端链路
4. 分析服务器日志确认调用链路

## 链路验证结果

### ✅ 通畅部分

| 链路 | 状态 | 数据 |
|------|------|------|
| 启动初始化 | ✅ | 56ms 完成（13模块加载） |
| 配置加载 | ✅ | YAML 读写一致（Bug T 修复后） |
| 数据库初始化 | ✅ | 8ms |
| 跨会话记忆恢复 | ✅ | 10条决策, 4实体, 1摘要 |
| 模型探测 | ✅ | qwen3.6-35b-a3b 已加载 |
| Handler 集成 | ✅ | ChatHandler + CommandHandler + TaskHandler |
| Dashboard API | ✅ | 返回完整状态（11个字段） |
| 模型列表 API | ✅ | 23个模型 |
| 会话管理 API | ✅ | 7个会话，CRUD 正常 |
| 上下文管理 | ✅ | enabled, effectiveWindow=348 |
| 审计日志 | ✅ | 4条事件 |
| 单元测试 | ✅ | 29/29 ALL PASS |

### ❌ 严重阻塞点

**P0: 意图解析链路断裂**

- **现象**: 发送 "ping" → LLM 解析为 `type=command confidence=0.9` → 走 handleCommand → 返回 "Unknown command: ing"
- **根因**:
  1. `quickParse("ping")` 匹配 `msg.length < 5` 但返回 `confidence: 0.7`，低于 0.9 阈值
  2. 走 `modelParse` 调用 LLM（耗时 55 秒）
  3. LLM 把 "ping" 误判为 command 类型
  4. `handleCommand("ping")` 没匹配任何命令，返回 "Unknown command"
- **影响**: 用户无法正常聊天，所有短消息（<5字且不匹配 chatPatterns）都会被误判

**P1: SSE 无超时保护（Bug U 未修复）**
- SSE 流式请求阻塞超过 2 分钟未返回
- `/api/chat/stream` 未使用 `chatTimeoutMs` 设置超时

**P1: 并发请求阻塞**
- LM Studio 串行推理，但服务器无请求队列/互斥锁
- 同时发送同步和流式 chat 请求会互相阻塞

**P1: 配置不一致**
- `GET /api/config` 返回 `model=qwen/qwen3.5-9b`
- `GET /api/models` 返回 `current=qwen/qwen3.6-35b-a3b`
- BreakIn 查询 `qwen3-1.7b` 画像，但实际模型是 `qwen3.6-35b-a3b`

## 修复内容

### Bug S（P0）: 意图解析误判导致 chat 链路断裂

**修复 1**: `agent-core.ts` sendMessage + sendMessageStream
- `case 'command':` 分支添加防御性检查
- 如果输入不以 `/` 开头，回退到 handleChat/handleChatStream
- 同时修复了 sendMessage（L470）和 sendMessageStream（L757）两处

**修复 2**: `intent-parser.ts` quickParse
- 短消息（<5字）的 `confidence` 从 `0.7` 提高到 `0.95`
- 避免不必要的 LLM 意图解析（节省 30-60 秒延迟）
- 短消息本身就是闲聊，不需要走 LLM 判断

### 验证

- ✅ `tsc -b packages/core --force` 编译通过
- ✅ 29/29 单元测试 ALL PASS
- ✅ Commit `88628fd`，已 push 到 main

## 链路设计评估

### 设计合理

1. **模块化分层清晰**: Config → Adapter → AgentCore → Handlers → LLM
2. **Handler 委托模式**: agent-core 保留 fallback，handler 存在时委托
3. **事件总线 + SSE**: 实时状态推送设计良好
4. **上下文管理器**: TF-IDF + 位置权重的注意力机制
5. **韧性保障**: 检查点 + 熔断器 + 重试引擎
6. **记忆系统**: 文件 + DB 双层，跨会话恢复

### 设计不合理

1. **意图解析阈值不合理**: 0.9 太高，导致大多数消息都要走 LLM
2. **handleCommand 无防御**: 不检查输入是否以 `/` 开头就执行命令解析（已修复）
3. **配置源混乱**: YAML 和 JSON 并存，容易不一致
4. **无请求互斥**: LM Studio 串行推理但服务器无队列
5. **SSE 无超时**: chatTimeoutMs 配置存在但未使用

## 修复优先级

| 优先级 | 问题 | 状态 |
|--------|------|------|
| 🔴 P0 | 意图解析断裂 | ✅ 已修复 |
| 🔴 P0 | handleCommand 无 / 前缀检查 | ✅ 已修复 |
| 🟡 P1 | SSE 无超时（Bug U） | ⏳ 待修复 |
| 🟡 P1 | 并发请求阻塞 | ⏳ 待修复 |
| 🟡 P1 | 配置不一致 | ⏳ 待修复 |
| 🟢 P2 | 模型探测不匹配 | ⏳ 待修复 |

## 本轮成果

- 链路评估文档: `link_health_check_20260709_0030.md`
- 2 个 P0 Bug 修复（意图解析 + 命令防御）
- 1 个性能优化（短消息避免 LLM 调用）
- Commit `88628fd` push 到 main

## 下一步建议

1. **修复 Bug U**: 在 `/api/chat/stream` 路由添加 AbortController 超时控制
2. **添加请求互斥**: LM Studio 调用前获取锁，避免并发阻塞
3. **统一配置源**: 废弃 `config/default.json`，只用 YAML
4. **修复模型探测**: BreakIn 应查询实际加载的模型名，而非配置中的模型名
