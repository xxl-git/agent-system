# P1/P2 批量修复总结

**日期**：2026-07-10
**Commit**：`99a7c3d`（批量修复）+ `1ecaafc`（并发互斥）+ push 成功 `88628fd..1ecaafc main -> main`

## 修复清单

### P1 — 高优先级

#### P1.1 测试覆盖扩展
- 为 9 个 npm 包添加基础单元测试：
  - `events`, `experience`, `llm`, `memory`, `models-core`, `prompts`, `resilience`, `skills`, `tools`
- 测试套件总数：8 → 17，全部通过
- 新增文件：`packages/*/src/__tests__/*.test.ts`（9 个）

#### P1.2 packages/server 死代码移除
- `packages/server/` 与活跃的 `src/server/` 仅 34% 相似，为旧版残留
- 移到 `docs/archive/packages-server-dead/`
- 工作区通配符 `packages/*` 仍匹配剩余 10 个活跃包

#### P1.3 废弃文件归档
- 89 个文件归档到 `docs/archive/`：
  - 3 个 backup/v2 文件（`agent-core.ts.backup`, `agent-core.ts.backup_20260623`, `command-handler-v2.ts`）
  - 2 个空测试 stub（`alpha.ts`, `beta.ts`）
  - 59 个临时/报告文档（`*_FIX_*.md`, `*_REVIEW_*.md`, `compile_*.txt`, `tsc_*.txt` 等）
  - 25 个 junk/temp 文件

#### P1.4 聊天请求并发互斥（commit `1ecaafc`）
- **问题**：LM Studio 串行推理，多个并发请求会互相阻塞，用户看不到任何反馈
- **修复**：
  - `/api/chat` 使用 `withChatMutex()` 串行化请求
  - `/api/chat/stream` 使用 `streamChatInProgress` 标志，第二个流式请求返回 429
  - 流式标志在 `.then` / `.catch` / `req.close` 三个路径都重置

### P2 — 中优先级

#### P2.1 配置字段名修正
- YAML `chattimeoutMs` → `chatTimeoutMs`（camelCase，与 TypeScript 类型定义一致）
- `agent-system-config.ts` 默认值同步更新

#### P2.2 maxTokens 字段重命名
- `lmstudio.maxTokens` → `maxOutputTokens`（语义清晰：是输出 token 上限，不是上下文窗口）
- `getEffectiveContextWindow()` 不再用 `maxTokens` 作为上下文 fallback（改为 `contextLength || 4096`）
- 影响 4 个文件：`agent-system.yaml`, `agent-system-config.ts`, `lmstudio.ts`, `dashboard-api.ts`

#### P2.3 BreakIn 模型名同步
- **问题**：`BreakInMachine` 构造时用配置 `qwen/qwen3.5-9b`，但 `onboardModel()` 探测到实际模型 `qwen3.6-35b-a3b` 后才调用 `setModelName()`
- **修复**：`onboardModel()` 在调用 `breakIn.onboard()` **前**执行 `setModelName(modelName)`

#### P2.4 /api/trace/list 实数据
- `getRecentTraces()` 已在 `@agent-system/resilience/tracer.ts` 实现但未导出
- 修复：`packages/resilience/src/index.ts` 添加导出
- 修复：`agent-server.ts` 调用 `tracerModule.getRecentTraces(20)` 替代空数组占位

#### P2.5 包 tsconfig.json 编码清理
- 9 个 `packages/*/tsconfig.json` 清理 BOM/乱码字节

## 验证结果

- **编译**：`tsc -b` 通过，0 错误
- **单元测试**：17/17 套件全部通过
- **配置验证**：`server.chatTimeoutMs=300000`, `lmstudio.maxOutputTokens=4096` 正确

## 未修复项

- **P3**: 模型列表无心跳机制——LM Studio 加载/卸载模型后需手动刷新 `/api/models/scan`
  - 影响：UI 模型下拉列表可能过期
  - 临时方案：UI 🔄 按钮手动刷新
