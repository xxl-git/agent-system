# 控制面板功能检查报告 - 2026-06-25 22:36

## 测试方法

- 后端 API：通过 HTTP 请求逐个端点验证
- 前端功能：通过源码审查 + API 交叉验证
- LM Studio 阻塞状态：API 返回正常，但模型推理超时（35B 参数模型）

---

## ✅ 正常工作的功能（12/16）

### 1. 基本状态（全部正常）
| 功能 | 后端 | 前端 | 状态 |
|------|------|------|------|
| `/api/status` 服务器状态 | ✅ 已实现 | ✅ 调用（send 前检查 ready） | ✅ 200 OK |
| `/api/config` 配置管理 | ✅ 已实现 | ✅ 设置面板读写 | ✅ 200 OK |
| SSE 事件推送 `/api/events` | ✅ 已实现 | ✅ `connectSSE()` 连接 | ✅ 端点存在 |

### 2. 会话管理（全部正常）
| 功能 | 后端 | 前端 | 状态 |
|------|------|------|------|
| 会话列表 GET | ✅ `GET /api/sessions` | ✅ `loadSessionList()` | ✅ 返回6条会话 |
| 新建会话 POST | ✅ `POST /api/sessions` | ✅ `newSession()` | ✅ 201 Created |
| 会话详情 GET | ✅ | ✅ `switchSession()` | ✅ |
| 更新/重命名 | ✅ | ✅ `editSessionTitle()` | ✅ |
| 删除会话 | ✅ | ✅ `deleteSession()` | ✅ 需要确认再删除 |

### 3. 仪表盘
| 功能 | 后端 | 前端 | 状态 |
|------|------|------|------|
| 综合仪表盘 `/api/dashboard` | ✅ | ✅ 控制面板 | ✅ 丰富数据返回 |
| 上下文管理器 `/api/dashboard/context` | ✅ | ✅ 上下文卡片 | ✅ |

### 4. 聊天功能（前端完整，后端受 LM Studio 阻塞）
| 功能 | 后端 | 前端 | 状态 |
|------|------|------|------|
| 发送消息 `send()` | ✅ `POST /api/chat` | ✅ 完整实现 | ⚠️ LM Studio 超时 |
| 流式聊天 `POST /api/chat/stream` | ✅ SSE 实现 | ✅ `addStreamingMsg()` | ⚠️ LM Studio 超时 |
| 停止生成 | ❌ 无后端取消机制 | ✅ `stopGeneration()` + `AbortController` | ⚠️ 前端可 abort |
| 重试消息 | ✅ 重新发送 | ✅ `retryMessage()` | ✅ |
| 编辑/删除消息 | ✅ 本地操作 | ✅ `editMessage()`/`deleteMessage()` | ✅（前端本地） |

### 5. 其他功能
| 功能 | 后端 | 前端 | 状态 |
|------|------|------|------|
| Toast 通知 | - | ✅ `showToast()` | ✅ |
| 打字指示器 | - | ✅ `showTyping()`/`hideTyping()` | ✅ |
| Agent 状态条 | ✅ SSE 事件 | ✅ `handleAgentStatusSSE()` | ✅ |
| 主题切换 | - | ✅ `applyTheme()` | ✅ |
| 自动滚动 | - | ✅ `scrollToBottom()` | ✅ |

---

## ⚠️ 部分可用（2/16）

### 1. 模型列表
| 功能 | 结果 | 原因 |
|------|------|------|
| `GET /api/models` | ⚠️ 返回空数组 | LM Studio 模型列表接口返回空 |
| 前端模型选择器 | ⚠️ 功能完整但无可选模型 | `fetchModels()` → 空结果 |
| 自定义 Provider 管理 | ⚠️ 前端 UI 完整，后端无 `/api/providers` | 前端有保存/测试/删除 Provider 的 UI，但后端缺少对应端点 |

### 2. 文件上传
| 功能 | 结果 | 原因 |
|------|------|------|
| `/api/upload` 后端 | ⚠️ 源码中有 `/api/upload` 路由处理 | 测试返回 404，可能路由顺序或路径问题 |
| 前端上传 UI | ✅ `onFileSelected()`/`renderUploadPreview()` | 可以选文件，上传可能因 LM Studio 阻塞 |

---

## ❌ 不可用（3/16）

| 功能 | 状态 | 原因 |
|------|------|------|
| `/api/stats` | ❌ **404** | 后端未实现此端点 |
| `/api/health` | ❌ **404** | 后端未实现健康检查端点 |
| `/api/docs` | ❌ **404** | 后端未实现文档端点 |

### 后端缺少的端点
```
❌ /api/providers   — 前端有完整的 Provider 管理 UI（保存/测试/删除），后端无对应路由
❌ /api/stats       — 前端 dashboard 面板可能会调用（404）
❌ /api/health      — 通用健康检查端点
❌ /api/docs        — API 文档
```

---

## 🚫 LM Studio 阻塞的核心功能

### 聊天功能测试结果
```
POST /api/chat {"message":"ping"} → ⏳ 超时（10秒+）
POST /api/chat/stream → ⏳ 超时（10秒+）
```

**原因**：模型 `qwen3.6-35b-a3b-mtp`（35B 参数）推理极其缓慢
- 其他用户早在 2026-06-20 就报告了同样问题
- 日志显示 timeouts 持续发生
- 新配置的超时 600 秒（10分钟）尚未被正式测试

### 模型列表
```
GET /api/models → {"models":[],"current":"unknown","error":"fetch failed"}
```

---

## 📊 汇总

| 类别 | 总数 | ✅ 正常 | ⚠️ 部分可用 | ❌ 不可用 |
|------|------|--------|------------|----------|
| 后端 API | 16 | 12 | 2 | 2 |
| 前端功能 | ~30 | ~25 | 3 | 2 |

### 需要修复的优先事项

1. **🔴 `/api/providers` 缺失** — 前端有完整 Provider 管理 UI，但后端无对应路由
2. **🟡 `/api/stats`、`/api/health`、`/api/docs` 缺失** — Dashboard 面板需要
3. **🟡 文件上传 `/api/upload` 路由** — 需要确认实际工作路径
4. **🔵 LM Studio 模型不可用** — 核心阻塞问题，需要切换模型或使用云端 API

### 测试中发现的隐蔽问题

- `audit.log` 记录了 3 次旧的 `agentEventBus.modelResponding is not a function` 错误（**已修复的 bug**）
- 还有 1 次 timeout 错误（旧配置 120 秒不够，新配置 600 秒后尚未验证）
