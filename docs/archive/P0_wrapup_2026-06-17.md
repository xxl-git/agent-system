# P0 收尾 — A型心跳 & E2E 集成测试

**日期**: 2026-06-17 10:45-10:55

## 完成内容

### P0-1: A 型心跳（空闲主动巡检）
- `agent-core.ts` init() 中接入 Orchestrator 心跳监听
- 新增 `onHeartbeat()` 方法，空闲时自动巡检：
  1. 待处理技能申请自动审核
  2. 活跃项目进度重算 + 进度条展示
  3. 熔断器恢复状态检查
  4. 待恢复检查点任务列表
- init() 末尾调用 `this.orchestrator.startHeartbeat()` 启动心跳
- 修改文件: `src/core/agent/agent-core.ts` — 3处修改

### P0-2: E2E 集成测试
- 新建 `tests/test-e2e.ts` — 14项测试，连接 LM Studio 验证全链路
- 新建 `tests/test-heartbeat.ts` — 7项测试，验证心跳机制

## 验证结果

### 心跳测试 (7/7 通过)
- 心跳启停生命周期 ✓
- 快频验证 (500ms 间隔, 1.2秒内触发2次) ✓
- 忙碌门控 (繁忙时跳过心跳) ✓
- 巡检内容 (项目进度、技能缺口、检查点) ✓
- 默认间隔 5min 验证 ✓
- 重复启动保护 (不创建第二个定时器) ✓

### E2E 集成测试 (14/14 通过)
- LM Studio 连通性 + 模型名获取 ✓
- 简单对话 (LLM 调用) ✓
- 韧性层4子系统全通 (健康监控/断路器/检查点/恢复编排) ✓
- 被保护对话 (executeProtected + LLM) ✓
- Token 流监控周期 ✓
- 离线模块验证 6项 (health/circuit/checkpoint/recovery/degradation/retry) ✓

## 文件清单
- `src/core/agent/agent-core.ts` — 修改: +heartbeat 事件监听 + onHeartbeat() + startHeartbeat()
- `tests/test-e2e.ts` — 新建: 14项测试
- `tests/test-heartbeat.ts` — 新建: 7项测试

## 修复的编译问题
- 心跳测试: SubTask 缺字段 (id/dependsOn/estimatedMinutes) → 补全
- 心跳测试: registerTask 少一个 arg (request) → 补第2参数
- 心跳测试: deleteProject 不存在 → 注释跳过
- E2E: save() 参数类型不匹配 (CompletedStep 结构) → 修正
- E2E: registerTask 缺 request → 补全
- 两个测试的 import 路径: `./` → `../src/` 前缀修正
